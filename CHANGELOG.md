# Changelog

All notable changes to `docs-drift` will be documented in this file.

The format is based on Keep a Changelog and this project follows Semantic Versioning.

## [0.1.0] - 2026-06-08

### Added

- Initial `docs-drift` monorepo with `core`, `cli`, and `github-action` packages
- Checks for documented CLI flags, environment variables, and config keys
- YAML-based config loading and validation
- GitHub Action summary output and CI workflow
- Node test suite covering parser, config validation, and scan behavior
