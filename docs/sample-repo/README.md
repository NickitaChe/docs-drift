# Sample Repo

This fixture demonstrates what `docs-drift` is expected to watch.

- CLI flags such as `--watch` and `--port`
- Environment variables such as `DATABASE_URL`
- Config keys such as `server.port`
